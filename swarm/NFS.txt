sudo apt update
sudo apt install nfs-kernel-server

sudo mkdir -p /srv/nfs/db-data
sudo chown -R nobody:nogroup /srv/nfs/db-data
sudo chmod -R 777 /srv/nfs/db-data 

sudo vim /etc/exports
/srv/nfs/db-data  *(rw,sync,no_subtree_check)

sudo exportfs -ra
sudo systemctl restart nfs-kernel-server

sudo exportfs -v

sudo mount -t nfs 10.4.41.37:/srv/nfs/db_data /mnt/nfs


volumes:
  db-data:
    driver: local
    driver_opts:
      type: "nfs"
      o: "addr=10.4.41.37,rw"
      device: ":/srv/nfs/db_data"